services:
  postgres:
    image: postgres:17
    container_name: trueCode_currency_pgdb
    restart: always
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: